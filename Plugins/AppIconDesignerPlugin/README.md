# AppIconDesignerPlugin

Design vector app icons with manual drawing tools and layer controls, then export an
`AppIcon.icon` for Xcode 26. Xcode renders the native macOS 26 appearance and
automatically generates compatible icon images when the app deploys to macOS 15.
