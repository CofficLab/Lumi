# 2026-07-31

## NewChatButton: 点击改为取消选择当前对话

将 `NewChatButton` 的点击行为从「创建新对话」改为「将 kernel 中当前选中的对话设为 nil」。

改动文件：
- `Packages/LumiKernel/Sources/LumiKernel/Providers/ConversationManaging.swift` — 协议新增 `deselectConversation()` 方法，extension 中提供默认空实现
- `Plugins/ConversationStorePlugin/Sources/Managers/ConversationManager.swift` — 实现 deselect：置 nil + updateCurrentTitle + persist + notify
- `Plugins/ConversationStorePlugin/Sources/Managers/ConversationService.swift` — 同上 + saveState
- `Plugins/ConversationStorePlugin/Sources/Managers/MockConversationManager.swift` — 同上
- `Plugins/ConversationNewPlugin/Sources/Views/NewChatButton.swift` — 移除 createConversation/errorAlert，点击直接调用 `kernel.conversations?.deselectConversation()`

## MessageListView: 增加未选择对话的占位视图

当 `selectedConversationID == nil` 时显示 `NoConversationSelectedView`，提示用户输入消息并回车来创建对话。

改动文件：
- `Plugins/MessageListPlugin/Sources/Views/NoConversationSelectedView.swift`（新建）— 图标 + 标题 + 提示文案，风格与 `MessageEmptyStateView` 一致
- `Plugins/MessageListPlugin/Sources/Views/MessageListView.swift` — body 的 if-else 链首条增加 `selectedConversationID == nil` 判断，优先于 isLoading/empty/scrollView
