# Views

视图层：所有 SwiftUI 视图。命名约定：`*View.swift`。
菜单栏专用视图见 `MenuBar/` 子目录。

| 文件 | 职责 |
| --- | --- |
| `SmartLightLogoView.swift` | 场景分发入口视图（按 `LogoScene` 选择动画 / 单色渲染） |
| `SmartLightAnimatedLogoView.swift` | 动画灯泡 Logo（`general` / `about` 场景） |
