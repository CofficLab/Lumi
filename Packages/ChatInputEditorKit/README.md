# ChatInputEditorKit

Reusable macOS chat input editor components.

The package owns the AppKit/SwiftUI editor bridge, keyboard handling, height
calculation helpers, cursor index conversion, and generic file drag/drop
callbacks. Application-specific chat state, logging, notifications, and plugin
integration stay in the app target.
