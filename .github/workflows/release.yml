name: Release

on:
  push:
    branches:
      - pre
      - main

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  # 基础环境变量
  SCHEME: Lumi
  REPO_LINK: "https://github.com/CofficLab/Lumi"
  DESTINATION: "generic/platform=macOS"
  ENTITLEMENTS_PATH: "LumiApp/App.entitlements"
  ArchivePath: "./my-app"
  BuildPath: "./temp"

  # Secrets
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
  BUILD_CERTIFICATE_P12_PASSWORD: ${{ secrets.BUILD_CERTIFICATE_P12_PASSWORD }}
  APP_STORE_CONNECT_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_KEY_BASE64 }}
  APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
  APP_STORE_CONNECT_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ISSUER_ID }}
  SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY_GITOK }}

jobs:
  release_pipeline:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.3"

      # =================================================================================================
      # 1. 版本计算与项目更新
      # =================================================================================================
      - name: Calculate Next Version
        id: version
        run: |
          chmod +x .github/scripts/bump-version.sh
          chmod +x .github/scripts/calculate-version.sh

          # 获取当前版本 (仅用于显示)
          LAST_TAG=$(git tag -l "v*" | sort -V | tail -n 1 2>/dev/null || echo "v0.0.0")
          LAST_TAG="${LAST_TAG#v}"

          # 计算新版本
          NEW_VERSION=$(.github/scripts/calculate-version.sh)
          echo "new_version=$NEW_VERSION" >> $GITHUB_ENV

          # 保存上一个标签供 changelog 生成使用
          echo "previous_tag=$LAST_TAG" >> $GITHUB_ENV

          # 输出摘要
          echo "## 📊 版本计算" >> $GITHUB_STEP_SUMMARY
          echo "- **上一个版本**: \`$LAST_TAG\`" >> $GITHUB_STEP_SUMMARY
          echo "- **新版本**: \`$NEW_VERSION\`" >> $GITHUB_STEP_SUMMARY

      - name: Update Xcode Project Version
        run: |
          # 更新 MARKETING_VERSION
          agvtool new-marketing-version ${{ env.new_version }} 2>/dev/null || true
          # 强制使用 sed 更新所有 target 的 MARKETING_VERSION 配置
          sed -i '' "s/MARKETING_VERSION = [^;]*/MARKETING_VERSION = ${{ env.new_version }}/" Lumi.xcodeproj/project.pbxproj

          # 验证（使用 xcodebuild 而非 agvtool）
          NEW_MARKETING_VERSION=$(xcodebuild -project ${{ env.SCHEME }}.xcodeproj -showBuildSettings 2>/dev/null | grep -E "MARKETING_VERSION" | grep -v "CURRENT" | head -1 | awk -F'= ' '{print $2}')
          echo "Updated Marketing Version: $NEW_MARKETING_VERSION"

      - name: Update Build Number
        run: |
          # 更新 CURRENT_PROJECT_VERSION
          agvtool next-version -all 2>/dev/null || true
          NEW_BUILD_VERSION=$(xcodebuild -project ${{ env.SCHEME }}.xcodeproj -showBuildSettings 2>/dev/null | grep "CURRENT_PROJECT_VERSION" | awk '{print $3}')

          # 强制使用 sed 同步所有 target 的 CURRENT_PROJECT_VERSION
          sed -i '' "s/CURRENT_PROJECT_VERSION = [^;]*/CURRENT_PROJECT_VERSION = $NEW_BUILD_VERSION/" Lumi.xcodeproj/project.pbxproj

          echo "Updated Build Version: $NEW_BUILD_VERSION"

      - name: Determine Tag and Prerelease Status
        run: |
          BUILD_VERSION=$(xcodebuild -project ${{ env.SCHEME }}.xcodeproj -showBuildSettings 2>/dev/null | grep "CURRENT_PROJECT_VERSION" | awk '{print $3}')

          if [[ "${{ github.ref }}" == "refs/heads/pre" ]]; then
            TAG_NAME="p${{ env.new_version }}($BUILD_VERSION)"
            echo "IS_PRERELEASE=true" >> $GITHUB_ENV
            echo "## 🏷️ Tag Info (Pre-release)" >> $GITHUB_STEP_SUMMARY
          else
            TAG_NAME="v${{ env.new_version }}"
            echo "IS_PRERELEASE=false" >> $GITHUB_ENV
            echo "## 🏷️ Tag Info (Release)" >> $GITHUB_STEP_SUMMARY
          fi

          echo "tag=$TAG_NAME" >> $GITHUB_ENV
          echo "- **Tag**: \`$TAG_NAME\`" >> $GITHUB_STEP_SUMMARY

      # =================================================================================================
      # 2. 构建与签名
      # =================================================================================================
      - name: Setup macOS signing
        id: signing
        run: |
          chmod +x ./.github/scripts/setup-macos-signing.sh
          source ./.github/scripts/setup-macos-signing.sh
          echo "signing_identity=$SIGNING_IDENTITY" >> $GITHUB_OUTPUT
          echo "team_id=$TEAM_ID" >> $GITHUB_OUTPUT
          echo "api_key_path=$API_KEY_PATH" >> $GITHUB_OUTPUT

      - name: Check Timestamp Service Availability
        run: |
          echo "🔍 Checking Apple timestamp service availability..."
          # Check if we can reach Apple's timestamp server
          for i in {1..3}; do
            if nc -z -w 5 timestamp.apple.com 80 2>/dev/null || \
               nc -z -w 5 ts.apple.com 80 2>/dev/null; then
              echo "✅ Timestamp service is reachable"
              exit 0
            fi
            echo "⚠️  Attempt $i: Timestamp service not reachable, retrying..."
            sleep 2
          done
          echo "❌ Timestamp service is not reachable after 3 attempts"
          exit 1

      - name: Install xcpretty
        run: gem install xcpretty

      - name: Restore CodeEditLanguages Framework
        id: codeedit-languages-cache
        uses: actions/cache@v4
        with:
          path: |
            Packages/CodeEditLanguages/CodeLanguagesContainer.xcframework
            Packages/CodeEditLanguages/CodeLanguagesContainer.xcframework.zip
          key: ${{ runner.os }}-codeedit-languages-${{ hashFiles('Packages/CodeEditLanguages/Package.resolved', 'Packages/CodeEditLanguages/CodeLanguages-Container/CodeLanguages-Container.xcodeproj/project.pbxproj', 'Packages/CodeEditLanguages/build_framework.sh') }}

      - name: Build CodeEditLanguages Framework
        if: steps.codeedit-languages-cache.outputs.cache-hit != 'true'
        run: |
          cd Packages/CodeEditLanguages
          ./build_framework.sh

      - name: Check CodeEditLanguages Framework
        run: scripts/check-codeedit-languages-framework.sh

      - name: Build App (Archive)
        run: |
          set -o pipefail
          xcodebuild archive \
            -scheme ${{ env.SCHEME }} \
            -archivePath ./temp/${{ env.SCHEME }}.xcarchive \
            -configuration Release \
            -skipPackagePluginValidation \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="-" \
            DEVELOPMENT_TEAM="${{ steps.signing.outputs.team_id }}" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            CODE_SIGNING_REQUIRED=NO \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            | xcpretty

      - name: Codesign App
        run: |
          # 从 xcarchive 中提取 App (使用 ditto 保持符号链接和元数据)
          ditto "./temp/${{ env.SCHEME }}.xcarchive/Products/Applications/${{ env.SCHEME }}.app" "./temp/${{ env.SCHEME }}.app"

          APP_PATH="./temp/${{ env.SCHEME }}.app"
          IDENTITY="${{ steps.signing.outputs.signing_identity }}"

          # 使用脚本进行深度签名
          chmod +x .github/scripts/sign_app.sh
          .github/scripts/sign_app.sh "$APP_PATH" "$IDENTITY" "${{ env.ENTITLEMENTS_PATH }}"

      - name: Create DMG
        run: |
          set -euo pipefail
          STAGING_DIR="./temp/dmg_staging"
          VOL_NAME="${{ env.SCHEME }}"
          DMG_NAME="./temp/${{ env.SCHEME }}_${{ env.new_version }}.dmg"

          # 干净 staging，避免上次失败或增量残留导致 hdiutil 异常
          rm -rf "$STAGING_DIR"
          mkdir -p "$STAGING_DIR"
          ditto "./temp/${{ env.SCHEME }}.app" "$STAGING_DIR/${{ env.SCHEME }}.app"
          ln -sfn /Applications "$STAGING_DIR/Applications"

          echo "Checking Staging Dir:"
          ls -la "$STAGING_DIR"

          # 降低 Spotlight 在 runner 上占用源目录导致 EBUSY 的概率
          mdutil -i off "$STAGING_DIR" 2>/dev/null || true
          mdutil -i off "./temp" 2>/dev/null || true
          sync
          sleep 1

          create_dmg() {
            # 同名卷若仍挂在 /Volumes（上次失败、并发、本地重跑），会导致 Resource busy
            hdiutil detach "/Volumes/${VOL_NAME}" -force 2>/dev/null || true
            rm -f "$DMG_NAME"
            if hdiutil create -volname "$VOL_NAME" -srcfolder "$STAGING_DIR" -ov -format UDZO "$DMG_NAME"; then
              return 0
            fi
            return 1
          }

          MAX_ATTEMPTS=6
          attempt=0
          until create_dmg; do
            attempt=$((attempt + 1))
            if [ "$attempt" -ge "$MAX_ATTEMPTS" ]; then
              echo "hdiutil create failed after $MAX_ATTEMPTS attempts (often Resource busy / EBUSY)"
              exit 1
            fi
            echo "hdiutil create failed (attempt $attempt/$MAX_ATTEMPTS), retrying after backoff..."
            sleep $((attempt * 3))
          done

          echo "Created: $DMG_NAME"

      - name: Notary
        run: |
          file=$(find ./temp -name "*.dmg" -type f | head -n 1)
          if [ -z "$file" ]; then echo "DMG not found"; exit 1; fi

          # Run submit and capture output
          set +e
          xcrun notarytool submit "$file" \
            --key ${{ steps.signing.outputs.api_key_path }} \
            --key-id=${{ env.APP_STORE_CONNECT_KEY_ID }} \
            --issuer ${{ env.APP_STORE_CONNECT_KEY_ISSUER_ID }} \
            --wait \
            --timeout 10m > notary_result.txt 2>&1

          EXIT_CODE=$?
          # Output result for debugging
          cat notary_result.txt

          if [ $EXIT_CODE -ne 0 ] || grep -q "status: Invalid" notary_result.txt; then
             echo "❌ Notarization failed. Retrieving logs..."
             # Extract ID from the output file
             SUBMISSION_ID=$(grep "id:" notary_result.txt | head -n 1 | awk '{print $2}' | tr -d ' ')
             if [ -n "$SUBMISSION_ID" ]; then
                echo "Fetching log for Submission ID: $SUBMISSION_ID"
                xcrun notarytool log "$SUBMISSION_ID" \
                  --key ${{ steps.signing.outputs.api_key_path }} \
                  --key-id=${{ env.APP_STORE_CONNECT_KEY_ID }} \
                  --issuer ${{ env.APP_STORE_CONNECT_KEY_ISSUER_ID }} 
             fi
             exit 1
          fi
          set -e

          stapler staple "$file"

      - name: Validate Notarization
        run: |
          file=$(find ./temp -name "*.dmg" -type f | head -n 1)
          stapler validate "$file"

      - name: Prepare Updates Directory
        run: |
          mkdir updates
          cp ./temp/*.dmg updates/
          ls -alh updates

      # =================================================================================================
      # 2.5 符号文件归档 (dSYM)
      # =================================================================================================
      - name: Package dSYMs
        run: |
          DSYM_SOURCE="./temp/${{ env.SCHEME }}.xcarchive/dSYMs"
          DSYM_ZIP="./temp/${{ env.SCHEME }}_${{ env.new_version }}_dSYMs.zip"

          if [ -d "$DSYM_SOURCE" ]; then
            cd "$DSYM_SOURCE"
            zip -r "$GITHUB_WORKSPACE/$DSYM_ZIP" ./*.dSYM
            echo "✅ dSYMs packaged: $(du -sh "$GITHUB_WORKSPACE/$DSYM_ZIP" | cut -f1)"
            ls -lh "$GITHUB_WORKSPACE/$DSYM_ZIP"
          else
            echo "⚠️ No dSYMs directory found in xcarchive"
            ls -R "./temp/${{ env.SCHEME }}.xcarchive/" || true
          fi

      # =================================================================================================
      # 3. Appcast 生成 (仅 Release 分支)
      # =================================================================================================
      - name: Generate Appcast
        if: env.IS_PRERELEASE == 'false'
        run: |
          # 解决依赖以获取 Sparkle 工具
          xcodebuild -resolvePackageDependencies
          SPARKLE_BIN=$(ls -d ~/Library/Developer/Xcode/DerivedData/${{ env.SCHEME }}*/SourcePackages/artifacts/sparkle/Sparkle/bin | head -n 1)

          if [ -d "$SPARKLE_BIN" ]; then
            cd "$SPARKLE_BIN"
            echo "${{ env.SPARKLE_PRIVATE_KEY }}" | ./generate_appcast --ed-key-file - ${{ github.workspace }}/updates
          else
            echo "❌ Sparkle bin not found"
            exit 1
          fi

      - name: Update Appcast Link
        if: env.IS_PRERELEASE == 'false'
        run: |
          cd updates
          if [ -f appcast.xml ]; then
            dmg_file=$(ls *.dmg 2>/dev/null | head -n 1)
            if [ -n "$dmg_file" ]; then
              # 对文件名进行 URL 编码处理（简单处理空格）
              dmg_url_encoded=$(echo "$dmg_file" | sed 's/ /%20/g')

              # 构建正确的下载链接（使用具体 tag 而不是 latest，确保永久有效）
              DOWNLOAD_URL="${{ env.REPO_LINK }}/releases/download/${{ env.tag }}/$dmg_url_encoded"

              # 替换 enclosure 标签中的 url 属性，无论当前 URL 是什么
              # 匹配 url="任意内容" 并替换为正确的链接
              sed -i '' 's|url="[^"]*"|url="'"$DOWNLOAD_URL"'"|g' "appcast.xml"

              # 移动到根目录以便提交
              mv appcast.xml ../appcast.xml

              echo "✅ Appcast updated with link: $DOWNLOAD_URL"
              echo "📝 Preview:"
              cat ../appcast.xml | head -n 20
            fi
          else
             echo "❌ appcast.xml generation failed"
             exit 1
          fi

      # =================================================================================================
      # 4. 提交更改与推送
      # =================================================================================================
      - name: Commit and Push Changes
        if: success()
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "ci: bump to ${{ env.new_version }}"
          commit_user_name: GitHub Action
          tagging_message: ${{ env.tag }}
          push_options: "--follow-tags"
          # 仅提交项目文件和appcast
          file_pattern: "Lumi.xcodeproj/project.pbxproj appcast.xml"

      # =================================================================================================
      # 5. 发布 Release
      # =================================================================================================
      - name: Generate Changelog
        if: success()
        run: |
          # 使用在 workflow 开始时保存的上一个标签
          # 这样可以避免 git-auto-commit-action 创建新 commit 后导致的标签混乱
          PREVIOUS_TAG="${{ env.previous_tag }}"

          if [ -z "$PREVIOUS_TAG" ] || [ "$PREVIOUS_TAG" = "0.0.0" ]; then
            NOTES="## Release ${{ env.tag }}\n\nInitial release"
          else
            NOTES="## Release ${{ env.tag }}\n\n### Changes since v$PREVIOUS_TAG\n"
            NOTES="$NOTES\n$(git log v${PREVIOUS_TAG}..HEAD --no-merges --pretty=format:'- %s' | sort | uniq | head -20)"
          fi

          echo -e "$NOTES" > changelog.md
          cat changelog.md >> $GITHUB_STEP_SUMMARY

      - name: Create GitHub Release
        if: success()
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.tag }}
          prerelease: ${{ env.IS_PRERELEASE }}
          body_path: changelog.md
          files: |
            ./updates/*.dmg
            ./appcast.xml
            ./temp/${{ env.SCHEME }}_${{ env.new_version }}_dSYMs.zip

  rebase:
    if: github.ref == 'refs/heads/main'
    needs:
      - release_pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Check pre branch
        id: check_pre
        run: |
          if git ls-remote --heads origin pre | grep -q pre; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi
      - name: Rebase pre on main
        if: steps.check_pre.outputs.exists == 'true'
        continue-on-error: true
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git fetch origin pre
          git checkout pre
          git rebase origin/main
          git push origin pre --force-with-lease
