---
alwaysApply: true
---

# 插件开发规范 (Plugin Development Guidelines)

## 核心原则

**所有插件必须遵循统一的代码结构、命名规范和实现模式，确保代码质量和可维护性。**

## 1. 插件文件结构

### 1.1 标准目录结构

```
LumiApp/Plugins/PluginNamePlugin/
├── PluginNamePlugin.swift      # 插件主文件（必需）
├── Models/                     # 数据模型（可选）
│   └── PluginModels.swift
├── Views/                      # 视图组件（可选）
│   ├── MainView.swift
│   └── SubView.swift
├── ViewModels/                 # 视图模型（可选）
│   └── PluginViewModel.swift
├── Services/                   # 服务层（可选）
│   └── PluginService.swift
└── Resources/                  # 资源文件（可选）
    └── Assets.xcassets
```

### 1.2 命名规范

- **插件目录**: `PluginNamePlugin` (PascalCase + Plugin 后缀)
- **主文件**: `PluginNamePlugin.swift` (与目录同名)
- **Actor 类名**: `PluginNamePlugin` (与文件名一致)
- **插件 ID**: `PluginName` (简洁的 PascalCase，不含 Plugin 后缀)
- **导航 ID**: `plugin_name` (snake_case，用于 URL 和标识)

## 2. 插件主文件结构

### 2.1 标准模板

```swift
import MagicKit
import SwiftUI

actor PluginNamePlugin: SuperPlugin, SuperLog {
    // MARK: - Plugin Properties
    
    nonisolated static let emoji = "🔌"
    nonisolated static let enable: Bool = true
    nonisolated static let verbose: Bool = false
    nonisolated static let isConfigurable: Bool = false
    
    static let id = "PluginName"
    static let navigationId = "plugin_name"
    static let displayName = String(localized: "Plugin Display Name")
    static let description = String(localized: "Plugin description text")
    static let iconName = "puzzlepiece.extension"
    static var order: Int { 100 }
    
    nonisolated var instanceLabel: String { Self.id }
    static let shared = PluginNamePlugin()
    
    // MARK: - Lifecycle
    
    nonisolated func onRegister() {
        // 初始化默认配置
    }
    
    nonisolated func onEnable() {
        Task { @MainActor in
            // 启动插件功能
        }
    }
    
    nonisolated func onDisable() {
        Task { @MainActor in
            // 清理插件资源
        }
    }
    
    // MARK: - UI
    
    @MainActor
    func addNavigationEntries() -> [NavigationEntry]? {
        return [
            NavigationEntry.create(
                id: Self.navigationId,
                title: Self.displayName,
                icon: Self.iconName,
                pluginId: Self.id
            ) {
                MainView()
            }
        ]
    }
}

// MARK: - Preview

#Preview("App") {
    ContentLayout()
        .hideSidebar()
        .withNavigation(PluginNamePlugin.navigationId)
        .inRootView()
        .withDebugBar()
}
```

### 2.2 必需属性说明

| 属性 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `emoji` | `String` | ✅ | 插件表情符号，用于 UI 展示 |
| `enable` | `Bool` | ✅ | 插件启用状态，默认 `true` |
| `verbose` | `Bool` | ✅ | 日志详细程度，默认 `false` |
| `id` | `String` | ✅ | 插件唯一标识符 |
| `navigationId` | `String` | ✅ | 导航标识符，使用 `id` 值即可 |
| `displayName` | `String` | ✅ | 显示名称，必须本地化 |
| `description` | `String` | ✅ | 描述文本，必须本地化 |
| `iconName` | `String` | ✅ | SF Symbols 图标名称 |
| `order` | `Int` | ✅ | 排序优先级，数字越小越靠前 |
| `instanceLabel` | `String` | ✅ | 实例标签，通常为 `Self.id` |
| `shared` | `Self` | ✅ | 单例实例 |

### 2.3 可选属性

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `isConfigurable` | `Bool` | `false` | 是否支持用户配置 |

## 3. 协议实现规范

### 3.1 必须实现的协议

```swift
// 基础插件协议
actor PluginNamePlugin: SuperPlugin {
    // 所有插件必须实现
}
```

### 3.2 推荐实现的协议

```swift
// 日志协议（推荐实现，便于调试）
actor PluginNamePlugin: SuperPlugin, SuperLog {
    nonisolated static let verbose = true
    // 可使用 self.t, self.log() 等日志方法
}
```

### 3.3 生命周期方法

```swift
// onRegister - 插件注册时调用（可选）
nonisolated func onRegister() {
    // 初始化默认配置
    if AppSettingsStore.shared.object(forKey: "SomeKey") == nil {
        AppSettingsStore.shared.set(defaultValue, forKey: "SomeKey")
    }
}

// onEnable - 插件启用时调用（可选）
nonisolated func onEnable() {
    Task { @MainActor in
        // 启动后台任务、监控等
    }
}

// onDisable - 插件禁用时调用（可选）
nonisolated func onDisable() {
    Task { @MainActor in
        // 停止后台任务、清理资源
    }
}
```

**注意**: 如果实现生命周期方法，即使方法体为空，也必须显式声明。

## 4. 代码组织规范

### 4.1 Import 顺序

```swift
import MagicKit      // 第三方框架优先
import SwiftUI       // Apple 框架在后
```

### 4.2 MARK 注释结构

```swift
// MARK: - Plugin Properties
// MARK: - Lifecycle
// MARK: - UI
// MARK: - Private Methods
// MARK: - Helpers
// MARK: - Preview
```

### 4.3 属性声明顺序

1. `emoji`
2. `enable`
3. `verbose`
4. `isConfigurable`
5. `id`
6. `navigationId`
7. `displayName`
8. `description`
9. `iconName`
10. `order`
11. `instanceLabel`
12. `shared`

### 4.4 类型注解规范

```swift
// ✅ 推荐：显式类型注解
nonisolated static let enable: Bool = true
nonisolated static let verbose: Bool = false

// ❌ 避免：依赖类型推断（公共 API）
nonisolated static let enable = true
```

### 4.5 order 属性规范

```swift
// ✅ 推荐：使用计算属性
static var order: Int { 100 }

// ❌ 避免：使用存储属性
static let order: Int = 100
```

## 5. UI 贡献规范

### 5.1 导航条目

```swift
@MainActor
func addNavigationEntries() -> [NavigationEntry]? {
    return [
        NavigationEntry.create(
            id: Self.navigationId,
            title: Self.displayName,
            icon: Self.iconName,
            pluginId: Self.id
        ) {
            MainView()
        }
    ]
}
```

### 5.2 状态栏弹窗（可选）

```swift
@MainActor
func addStatusBarPopupView() -> AnyView? {
    return AnyView(StatusBarPopupView())
}
```

### 5.3 无导航界面的插件

对于仅提供状态栏或后台功能的插件，`navigationId` 使用与 `id` 相同的值即可，系统会根据实际情况处理：

```swift
static let id = "MemoryManager"
static let navigationId = "MemoryManager"  // 与 id 相同

@MainActor
func addNavigationEntries() -> [NavigationEntry]? {
    return nil  // 不提供导航条目
}
```

## 6. 国际化规范

### 6.1 显示名称和描述

```swift
// ✅ 正确：使用 String(localized:)
static let displayName = String(localized: "Clipboard Manager")
static let description = String(localized: "Manage clipboard history and snippets")

// ❌ 错误：使用硬编码字符串
static let displayName = "Clipboard Manager"
```

### 6.2 本地化表（可选）

```swift
// 指定本地化表
static let displayName = String(localized: "Memory Monitor", table: "MemoryManager")
static let description = String(localized: "Real-time monitoring", table: "MemoryManager")
```

### 6.3 Localizable.xcstrings

在 `LumiApp/Core/Localizable.xcstrings` 中添加：

```json
"Clipboard Manager" : {
  "localizations" : {
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "Clipboard Manager"
      }
    },
    "zh-Hans" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "剪贴板管理器"
      }
    }
  }
}
```

## 7. 测试规范

### 7.1 单元测试（推荐）

```swift
import Testing
@testable import LumiApp

struct PluginNamePluginTests {
    @Test func testPluginInitialization() async {
        let plugin = PluginNamePlugin.shared
        #expect(PluginNamePlugin.enable == true)
        #expect(PluginNamePlugin.id == "PluginName")
    }
}
```

### 7.2 预览测试

```swift
// MARK: - Preview

#Preview("App") {
    ContentLayout()
        .hideSidebar()
        .withNavigation(PluginNamePlugin.navigationId)
        .inRootView()
        .withDebugBar()
}

#Preview("View Only") {
    MainView()
        .frame(width: 400, height: 300)
}
```

## 8. 禁用插件规范

### 8.1 临时禁用

```swift
// 开发中或未完成的插件应设置 enable = false
nonisolated static let enable: Bool = false
```

### 8.2 禁用插件清单

以下情况应将插件设置为禁用状态：
- 功能未完成
- 存在已知严重 bug
- 依赖外部服务不可用
- 性能问题待优化
- 安全审计未完成

## 9. 常见错误

### ❌ 错误示例

```swift
// 1. 缺少必需属性
actor BadPlugin: SuperPlugin {
    static let id = "BadPlugin"
    // 缺少 emoji, enable, displayName, description, iconName, order 等
}

// 2. 协议实现不一致
actor BadPlugin: SuperPlugin {
    // 应该实现 SuperLog 但没有
}

// 3. import 顺序混乱
import SwiftUI
import Foundation
import MagicKit
import OSLog

// 4. order 使用存储属性
static let order: Int = 100  // 应该用计算属性

// 5. 缺少 shared 单例
actor BadPlugin: SuperPlugin {
    // 缺少 static let shared
}

// 6. 生命周期方法不完整
actor BadPlugin: SuperPlugin {
    nonisolated func onEnable() { }
    // 缺少 onRegister 和 onDisable
}

// 7. navigationId 使用可选类型
static let navigationId: String? = "plugin_name"  // 应该用 String
```

### ✅ 正确示例

```swift
import MagicKit
import SwiftUI

actor GoodPlugin: SuperPlugin, SuperLog {
    // MARK: - Plugin Properties
    
    nonisolated static let emoji = "✨"
    nonisolated static let enable: Bool = true
    nonisolated static let verbose: Bool = false
    nonisolated static let isConfigurable: Bool = false
    
    static let id = "GoodPlugin"
    static let navigationId = "good_plugin"
    static let displayName = String(localized: "Good Plugin")
    static let description = String(localized: "A well-structured plugin")
    static let iconName = "star"
    static var order: Int { 50 }
    
    nonisolated var instanceLabel: String { Self.id }
    static let shared = GoodPlugin()
    
    // MARK: - Lifecycle
    
    nonisolated func onRegister() {
        // 初始化配置
    }
    
    nonisolated func onEnable() {
        Task { @MainActor in
            // 启动功能
        }
    }
    
    nonisolated func onDisable() {
        Task { @MainActor in
            // 清理资源
        }
    }
    
    // MARK: - UI
    
    @MainActor
    func addNavigationEntries() -> [NavigationEntry]? {
        return [
            NavigationEntry.create(
                id: Self.navigationId,
                title: Self.displayName,
                icon: Self.iconName,
                pluginId: Self.id
            ) {
                GoodPluginView()
            }
        ]
    }
}

// MARK: - Preview

#Preview("App") {
    ContentLayout()
        .hideSidebar()
        .withNavigation(GoodPluginPlugin.navigationId)
        .inRootView()
        .withDebugBar()
}
```

## 10. 代码审查清单

创建或修改插件时，请检查：

- [ ] 遵循标准目录结构
- [ ] 实现所有必需属性
- [ ] `navigationId` 使用 `String` 类型（不是 `String?`）
- [ ] 统一使用 `SuperLog` 协议（如需要日志）
- [ ] `enable` 属性有显式类型注解
- [ ] `order` 使用计算属性
- [ ] import 顺序正确（MagicKit → SwiftUI）
- [ ] 显示名称和描述已本地化
- [ ] 添加了 Preview 预览
- [ ] 生命周期方法完整（或全部省略）
- [ ] `shared` 单例已定义
- [ ] `instanceLabel` 已实现
- [ ] `emoji` 已定义

## 总结

遵循本规范可以确保：
- 📦 所有插件具有一致的代码结构
- 🔍 更容易进行代码审查和维护
- 🧪 更容易编写自动化测试
- 📚 新成员更容易理解和贡献
- 🛠️ 工具链更容易集成和检查
